# Core Flask Ecosystem (Flask 3.x compatible)
Flask==3.0.2  # Latest version that works with Flask-JWT-Extended
Werkzeug==3.0.1
Jinja2==3.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# Database & ORM (updated for Flask 3 compatibility)
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.25
greenlet==3.0.3
psycopg2-binary==2.9.9

# API & Security (compatible versions)
flask-cors==4.0.0  # Flask 3.x compatible version
Flask-JWT-Extended==4.5.2
PyJWT==2.8.0

# Environment & Utilities
python-dotenv==1.0.1
typing_extensions==4.8.0  # Pinned to avoid torch conflicts
pytz==2024.1
python-dateutil==2.8.2

# Production Server
gunicorn==21.2.0

# Development & Testing (optional)
pytest==8.1.1
pytest-cov==4.1.0
Flask-DebugToolbar==0.14.1
